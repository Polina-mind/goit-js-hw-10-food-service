import './js/templating';

import './styles.scss';

// localStorage.setItem('test', 'my test');

// const menuRef = document.querySelector('.js-menu');
// const element = '<li>Hello</li>';
// menuRef.innerHTML = element;
// menuRef.insertAdjacentHTML('beforeend', element);

// const markup = menu.map(el => `<li>${el}</li>`).join('');
// console.log(markup);

// menuRef.insertAdjacentHTML('beforeend', markup);
